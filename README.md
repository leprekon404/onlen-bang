# 🏦 Online Banking - Система онлайн-банкинга

Простая и безопасная система онлайн-банкинга на Node.js + MySQL с модулем управления счетами.

## 📋 Содержание
- [Возможности](#-возможности)
- [Требования](#-требования)
- [Установка](#-установка)
- [Запуск проекта](#-запуск-проекта)
- [Структура проекта](#-структура-проекта)
- [API документация](#-api-документация)
- [Тестовые пользователи](#-тестовые-пользователи)

---

## ✨ Возможности

### Модуль аутентификации
- ✅ Регистрация и вход пользователей
- ✅ JWT токены для авторизации
- ✅ Хеширование паролей (bcrypt)
- ✅ Защита от брутфорса

### Модуль управления счетами
- ✅ Просмотр всех счетов пользователя
- ✅ Отслеживание баланса в реальном времени
- ✅ История транзакций с фильтрами и пагинацией
- ✅ Создание новых счетов (дебетовые, кредитные, накопительные)
- ✅ Закрытие счетов с проверкой баланса
- ✅ Установка дневных лимитов
- ✅ Заморозка/разморозка счетов

### Модуль транзакций
- ✅ Переводы между счетами
- ✅ Пополнение и снятие средств
- ✅ Статусы транзакций (pending, completed, failed)

---

## 🔧 Требования

Перед установкой убедитесь, что у вас установлены:

- **Node.js** версии 16.x или выше ([Скачать](https://nodejs.org/))
- **MySQL** версии 8.0 или выше ([Скачать](https://dev.mysql.com/downloads/mysql/))
- **npm** или **yarn** (устанавливается вместе с Node.js)

Проверить установку:
```bash
node --version
npm --version
mysql --version
```

---

## 📦 Установка

### Шаг 1: Клонирование репозитория

```bash
git clone https://github.com/leprekon404/onlen-bang.git
cd onlen-bang
```

### Шаг 2: Установка зависимостей

```bash
npm install
```

Будут установлены следующие пакеты:
- `express` - веб-фреймворк
- `mysql2` - драйвер MySQL
- `bcryptjs` - хеширование паролей
- `jsonwebtoken` - JWT токены
- `dotenv` - переменные окружения
- `cors` - CORS политика
- `nodemon` - автоперезагрузка при разработке

### Шаг 3: Настройка базы данных

#### 3.1. Запустите MySQL сервер

**Windows:**
```bash
# Через службы Windows или из командной строки
net start MySQL80
```

**Linux/Mac:**
```bash
sudo systemctl start mysql
# или
sudo service mysql start
```

#### 3.2. Войдите в MySQL

```bash
mysql -u root -p
```

#### 3.3. Создайте базу данных и таблицы

Выполните SQL скрипт из корня проекта:

```bash
mysql -u root -p < database_schema.sql
```

Или вручную в MySQL консоли:

```sql
source database_schema.sql
```

Это создаст:
- База данных `online_banking_db`
- Пользователь `banking_app_user` с паролем `SecureP@ssw0rd2025!`
- Таблицы: `users`, `accounts`, `transactions`, `biometric_credentials`
- Тестовые данные (3 пользователя, 4 счета, 2 транзакции)

### Шаг 4: Настройка переменных окружения

Файл `.env` уже создан в проекте. Проверьте и при необходимости измените настройки:

```env
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=123456789          # Ваш пароль MySQL
DB_NAME=online_banking_db
DB_PORT=3306

JWT_SECRET=YourSuperSecretKeyChangeInProduction123!@#
JWT_EXPIRES_IN=24h

PORT=3000
NODE_ENV=development
```

**⚠️ ВАЖНО:** 
- Измените `DB_PASSWORD` на ваш пароль от MySQL root
- В продакшене обязательно измените `JWT_SECRET` на случайную строку

---

## 🚀 Запуск проекта

### Режим разработки (с автоперезагрузкой)

```bash
npm run dev
```

### Продакшн режим

```bash
npm start
```

### Проверка подключения к БД

Можете запустить тестовый скрипт:

```bash
node test-db.js
```

Если всё настроено правильно, вы увидите:

```
==============================
🏦 Online banking: http://localhost:3000
==============================
```

Откройте браузер и перейдите по адресу: **http://localhost:3000**

---

## 📁 Структура проекта

```
onlen-bang/
├── backend/
│   ├── config/
│   │   └── database.js          # Подключение к MySQL
│   ├── middleware/
│   │   └── auth.js              # JWT middleware
│   ├── routes/
│   │   ├── auth.js              # Маршруты аутентификации
│   │   ├── accounts.js          # Маршруты управления счетами
│   │   └── transactions.js      # Маршруты транзакций
│   └── server.js                # Главный файл сервера
├── frontend/
│   ├── login.html               # Страница входа
│   ├── dashboard.html           # Личный кабинет
│   └── styles.css               # Стили
├── docs/
│   └── API_ACCOUNTS.md          # API документация для счетов
├── database_schema.sql          # SQL схема БД
├── .env                         # Переменные окружения
├── package.json                 # Зависимости проекта
└── README.md                    # Этот файл
```

---

## 📚 API документация

### Базовый URL
```
http://localhost:3000/api
```

### Основные эндпоинты

#### Аутентификация
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход
- `POST /api/auth/logout` - Выход

#### Счета
- `GET /api/accounts` - Список счетов
- `GET /api/accounts/:id` - Информация о счете
- `GET /api/accounts/:id/balance` - Баланс счета
- `GET /api/accounts/:id/transactions` - История транзакций
- `POST /api/accounts` - Создать счет
- `PATCH /api/accounts/:id` - Обновить счет
- `DELETE /api/accounts/:id` - Закрыть счет
- `GET /api/accounts/user/summary` - Сводка по счетам

#### Транзакции
- `POST /api/transactions/transfer` - Перевод между счетами
- `POST /api/transactions/deposit` - Пополнение
- `POST /api/transactions/withdraw` - Снятие

**Подробная документация:** [docs/API_ACCOUNTS.md](docs/API_ACCOUNTS.md)

---

## 👤 Тестовые пользователи

После выполнения `database_schema.sql` в БД будут созданы тестовые пользователи:

| Username | Email | Пароль | Счета |
|----------|-------|--------|-------|
| ivanov | ivanov@bank.ru | Password123! | 1 дебетовый (150,000₽) |
| petrov | petrov@bank.ru | Password123! | 2 счета (75,000₽ + 25,000₽) |
| sidorov | sidorov@bank.ru | Password123! | 1 дебетовый (25,000₽) |

### Пример входа:

1. Откройте http://localhost:3000
2. Введите:
   - **Username:** `petrov`
   - **Password:** `Password123!`
3. Нажмите «Войти»

---

## 🔐 Безопасность

- ✅ Пароли хешируются с помощью bcrypt (12 раундов)
- ✅ JWT токены с истечением срока действия
- ✅ Защита от SQL инъекций (prepared statements)
- ✅ CORS политика
- ✅ Проверка принадлежности счетов пользователю
- ✅ Валидация входных данных

---

## 🛠️ Разработка

### Генерация хеша пароля

```bash
node gen-hash.js
```

### Просмотр логов

Логи выводятся в консоль при запуске в режиме разработки:
```bash
npm run dev
```

### Остановка сервера

Нажмите `Ctrl + C` в терминале

---

## 🐛 Решение проблем

### Ошибка подключения к MySQL

**Проблема:** `Error: connect ECONNREFUSED 127.0.0.1:3306`

**Решение:**
1. Проверьте, что MySQL сервер запущен:
   ```bash
   # Windows
   net start MySQL80
   
   # Linux/Mac
   sudo systemctl status mysql
   ```

2. Проверьте порт в `.env` (по умолчанию 3306)

### Ошибка авторизации в MySQL

**Проблема:** `Error: Access denied for user 'root'@'localhost'`

**Решение:**
1. Убедитесь, что пароль в `.env` совпадает с паролем MySQL
2. Попробуйте сбросить пароль root в MySQL

### Порт 3000 занят

**Проблема:** `Error: listen EADDRINUSE: address already in use :::3000`

**Решение:**
1. Измените порт в `.env`:
   ```env
   PORT=3001
   ```
2. Или остановите процесс на порту 3000:
   ```bash
   # Windows
   netstat -ano | findstr :3000
   taskkill /PID <PID> /F
   
   # Linux/Mac
   lsof -ti:3000 | xargs kill -9
   ```

### Модули не найдены

**Проблема:** `Error: Cannot find module 'express'`

**Решение:**
```bash
rm -rf node_modules package-lock.json
npm install
```

---

## 📝 Лицензия

MIT License

---

## 👨‍💻 Автор

**Anton** (leprekon404)
- GitHub: [@leprekon404](https://github.com/leprekon404)

---

## 🤝 Вклад в проект

Вклад приветствуется! Создавайте issues и pull requests.

1. Fork проекта
2. Создайте ветку для фичи (`git checkout -b feature/AmazingFeature`)
3. Commit изменения (`git commit -m 'Add some AmazingFeature'`)
4. Push в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

---

## 📧 Контакты

Если у вас есть вопросы или предложения, создайте issue в репозитории.
