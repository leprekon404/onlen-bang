# 🏦 Online Banking System

Полнофункциональная система онлайн-банкинга на Node.js + PostgreSQL + Express

## ✨ Возможности

### Для пользователей:
- 👤 Регистрация и авторизация с JWT
- 💳 Управление счетами и картами
- 💸 Переводы между счетами
- 📈 Аналитика доходов и расходов
- 🎯 Бюджеты и финансовые цели
- 🔔 Уведомления в реальном времени
- 🔑 API ключи для интеграций

### Для администраторов:
- 🛡️ Полная админ-панель
- 👥 Управление пользователями
- 📊 Общая статистика банка
- 🔒 Блокировка аккаунтов
- 💰 Пополнение счетов
- 📝 Логи всех операций

## 🚀 Быстрый старт

### 1. Установка PostgreSQL

**Windows:**
```bash
# Скачайте с https://www.postgresql.org/download/windows/
```

**Linux:**
```bash
sudo apt update
sudo apt install postgresql postgresql-contrib
```

**macOS:**
```bash
brew install postgresql
brew services start postgresql
```

### 2. Создание базы данных

```bash
# Войдите в PostgreSQL
psql -U postgres

# Создайте базу
CREATE DATABASE onlinebank;

# Подключитесь
\c onlinebank

# Запустите схему
\i backend/config/schema.sql

# Выйдите
\q
```

### 3. Настройка проекта

```bash
# Клонируйте репозиторий
git clone https://github.com/leprekon404/onlen-bang.git
cd onlen-bang

# Установите зависимости
cd backend
npm install

# Создайте .env файл
cp .env.example .env

# Отредактируйте .env с вашими данными
nano .env
```

### 4. Запуск

```bash
# Запустите сервер
node server.js

# Или в режиме разработки
npm run dev
```

### 5. Откройте в браузере

```
http://localhost:3000
```

## 🔑 Доступ администратора

**URL:** `http://localhost:3000/admin.html`

**Логин:** `admin`  
**Пароль:** `admin123`

## 📚 Структура проекта

```
onlen-bang/
├── backend/
│   ├── config/
│   │   ├── database.js      # PostgreSQL конфиг
│   │   └── schema.sql       # Схема БД
│   ├── middleware/
│   │   ├── auth.js          # Аутентификация
│   │   └── adminAuth.js     # Права админа
│   ├── routes/
│   │   ├── auth.js          # Авторизация
│   │   ├── accounts.js      # Счета
│   │   ├── transactions.js  # Транзакции
│   │   ├── analytics.js     # Аналитика
│   │   ├── admin.js         # Админ-панель
│   │   └── ...
│   ├── server.js            # Главный файл
│   ├── package.json
│   └── .env.example
└── frontend/
    ├── login.html           # Вход/Регистрация
    ├── dashboard.html       # Главная
    ├── admin.html           # Админ-панель
    ├── analytics.html       # Аналитика
    └── js/
        ├── dashboard.js
        ├── admin.js
        └── ...
```

## 🔧 Технологии

- **Backend:** Node.js, Express.js
- **Database:** PostgreSQL 15+
- **Authentication:** JWT (JSON Web Tokens)
- **Security:** bcryptjs, CORS
- **Frontend:** Vanilla JavaScript, Bootstrap 5
- **Real-time:** WebSocket готовность

## 📊 API Эндпоинты

### Авторизация
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход

### Счета
- `GET /api/accounts` - Список счетов
- `POST /api/accounts` - Создать счет
- `GET /api/accounts/:id` - Информация о счете

### Транзакции
- `POST /api/transactions/transfer` - Перевод
- `GET /api/transactions/history` - История

### Аналитика
- `GET /api/analytics/summary` - Общая статистика
- `GET /api/analytics/spending-by-category` - По категориям
- `GET /api/analytics/income-expense-trend` - Тренд

### Админ (требуется роль admin)
- `GET /api/admin/stats` - Статистика банка
- `GET /api/admin/users` - Список пользователей
- `GET /api/admin/transactions` - Все транзакции
- `PATCH /api/admin/users/:id/block` - Блокировка
- `POST /api/admin/users/:id/add-balance` - Пополнение

## 🐛 Решение проблем

См. файл [MIGRATION_TO_POSTGRES.md](backend/MIGRATION_TO_POSTGRES.md)

## 📝 Лицензия

ISC

## 👨‍💻 Автор

Anton (@leprekon404)
