# Установка на Windows

## Шаг 1: Установка предварительных требований

### 1.1 Node.js

1. Скачайте Node.js LTS: https://nodejs.org/
2. Запустите установщик
3. Проверьте установку:
   ```cmd
   node --version
   npm --version
   ```

### 1.2 PostgreSQL

1. Скачайте PostgreSQL: https://www.postgresql.org/download/windows/
2. Запустите установщик
3. Запомните пароль для пользователя `postgres`
4. Установите порт: `5432` (по умолчанию)
5. Установите pgAdmin (графический интерфейс)

### 1.3 Git

1. Скачайте Git: https://git-scm.com/download/win
2. Запустите установщик (оставьте все по умолчанию)

## Шаг 2: Клонирование проекта

1. Откройте **PowerShell** или **Command Prompt**
2. Перейдите в папку, куда хотите склонировать проект:

```cmd
cd C:\Projects
git clone https://github.com/leprekon404/onlen-bang.git
cd onlen-bang
```

## Шаг 3: Установка зависимостей

```cmd
npm install
```

## Шаг 4: Настройка .env файла

1. Скопируйте `.env.example`:
   ```cmd
   copy .env.example .env
   ```

2. Откройте `.env` в блокноте или любом редакторе
3. Укажите ваш пароль PostgreSQL:
   ```env
   DB_PASSWORD=ваш_пароль
   ```

## Шаг 5: Настройка базы данных

### Вариант A: Через pgAdmin (Рекомендуется)

1. **Откройте pgAdmin 4**
   - Найдите в меню Пуск или на рабочем столе

2. **Подключитесь к серверу**
   - Найдите `Servers` в левой панели
   - Кликните на `PostgreSQL`
   - Введите пароль

3. **Создайте базу данных**
   - Правый клик на `Databases` → `Create` → `Database...`
   - Имя: `online_banking_db`
   - Нажмите `Save`

4. **Откройте Query Tool**
   - Кликните на базу `online_banking_db`
   - Нажмите иконку молнии (⚡) в верхней панели - Query Tool

5. **Выполните SQL файлы по порядку:**

   **Шаг 5.1:** Откройте `database_schema_init.sql`
   - Файл находится в корне проекта
   - Откройте его в блокноте
   - Скопируйте весь текст (Ctrl+A, Ctrl+C)
   - Вставьте в Query Tool (Ctrl+V)
   - Нажмите F5 или кнопку ▶ Execute/Refresh
   - Дождитесь "✅ База данных... созданы"

   **Шаг 5.2:** Очистите Query Tool (Ctrl+A, Delete)
   
   **Шаг 5.3:** Откройте и выполните `database_schema.sql`
   - Скопируйте и вставьте
   - Нажмите F5
   - Дождитесь "✅ Основные таблицы созданы"

   **Шаг 5.4:** Очистите и выполните `database_schema_analytics.sql`
   
   **Шаг 5.5:** Очистите и выполните `database_schema_notifications.sql`
   
   **Шаг 5.6:** Очистите и выполните `database_schema_payments.sql`

6. **Проверьте результат**
   - В левой панели: `online_banking_db` → `Schemas` → `public` → `Tables`
   - Должно быть 15+ таблиц

### Вариант B: Через командную строку

Откройте **PowerShell от имени администратора** и выполните:

```powershell
# Замените 16 на вашу версию PostgreSQL
$psql = "C:\Program Files\PostgreSQL\16\bin\psql.exe"

cd C:\Projects\onlen-bang

& $psql -U postgres -f database_schema_init.sql
& $psql -U postgres -d online_banking_db -f database_schema.sql
& $psql -U postgres -d online_banking_db -f database_schema_analytics.sql
& $psql -U postgres -d online_banking_db -f database_schema_notifications.sql
& $psql -U postgres -d online_banking_db -f database_schema_payments.sql
```

## Шаг 6: Запуск приложения

1. Вернитесь в папку проекта
2. Запустите:

```cmd
npm run dev
```

3. Откройте браузер: **http://localhost:3000**

## Тестовые аккаунты

| Логин | Пароль |
|--------|----------|
| ivanov | Password123! |
| petrov | Password123! |
| sidorov | Password123! |

## Частые проблемы

### Ошибка: "psql не является командой"

**Решение:** Используйте pgAdmin (Вариант A)

### Ошибка: "Connection refused"

**Проверьте:**
1. PostgreSQL запущен:
   - Откройте Диспетчер задач (Ctrl+Shift+Esc)
   - Службы → найдите `postgresql-x64-XX`
   - Должно быть "Выполняется"

2. Проверьте пароль в `.env`

### Ошибка: "роль banking_app_user не существует"

**Решение:**
1. Выполните сначала `database_schema_init.sql`
2. Затем остальные файлы

### Порт 3000 занят

Измените в `.env`:
```
PORT=3001
```

### Кириллица отображается некорректно

Проверьте кодировку в редакторе - должна быть UTF-8

## Полезные команды

```cmd
# Запустить в режиме разработки
npm run dev

# Остановить сервер
Ctrl+C

# Проверить подключение к БД
node test-db.js

# Сгенерировать хеш пароля
node gen-hash.js
```

## Полезные ссылки

- 🏠 Главная: http://localhost:3000
- 🔐 Admin: http://localhost:3000/admin.html  
- 📡 API Status: http://localhost:3000/api/external/status
- 📖 Полная документация: README.md

---

**Готово!** 🎉 Если все прошло успешно, вы увидите страницу входа в браузере.
